# Root config for python -m adforce.eval.harvest (remote -> laptop rsync).
hydra:
  output_subdir: null
  run:
    dir: .

remote: ??? # host:/path/to/exp/eval (parent of study dirs), e.g. archer2:/work/n02/n02/sithom/exp/eval
study: ??? # study subtree to pull
dest: null # local mirror root (null -> data/comp/runs/<study>)
dry_run: true # print the rsync command without running it
